#include <iostream>
#include "Stack.h"
#include <sstream>

using namespace std;


void inverterPalavara(string &mensagem){
    stringstream ss(mensagem);

    string token;

    while(ss >> token){
        Stack <char> pinha;
        for(int i=0;)
    }
}

int main() {
    Stack<int> pilha;

    for(int i=9;i>0;i--){
        pilha.push(i);
    }

    for(auto it = ; )

    return 0;
}